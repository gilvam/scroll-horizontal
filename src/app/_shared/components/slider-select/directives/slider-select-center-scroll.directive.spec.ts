import { SliderSelectCenterScrollDirective } from './slider-select-center-scroll.directive';

describe('SliderSelectCenterDirective', () => {
	it('should create an instance', () => {
		const directive = new SliderSelectCenterScrollDirective();
		expect(directive).toBeTruthy();
	});
});
