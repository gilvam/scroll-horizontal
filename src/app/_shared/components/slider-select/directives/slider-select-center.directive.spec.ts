import { SliderSelectCenterDirective } from './slider-select-center.directive';

describe('SliderSelectCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderSelectCenterDirective();
    expect(directive).toBeTruthy();
  });
});
